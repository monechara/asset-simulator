# 人生全体マネープランシミュレーター 実装計画・データ構造・計算仕様書

著者: **Manus AI**  
作成日: 2026年8月11日  

---

## 第1部：現状監査と変更方針の整理

### 1. 現在のアプリの構成・機能・計算ロジック
現行アプリは、React 19とTailwind CSSを用いたSPAであり、月単位の複利計算エンジン（`simulator.ts`）と、単一ページのフォーム・結果画面（`Home.tsx`, `SimulatorForm.tsx`, `SimulatorResult.tsx`）で構成されています。計算の数学的精度は高いものの、「毎月の定額積立と運用利回り」のみに特化しており、人生の重大なキャッシュフロー変動（結婚、住宅、子ども、教育、老後）を捉えることができません。

### 2. 既存コードのうち再利用する部分
* **通貨フォーマット関数 (`formatCurrency`)**: 日本円の万円・億円表記変換ロジック。
* **Rechartsグラフ描画基盤**: エリアチャートやラインチャートのレスポンシブコンテナおよびツールチップ設定。
* **UI基本コンポーネント群**: Shadcn UIベースのカード、ボタン、スライダー、インプット等のデザインパーツ。
* **アニメーション基盤 (`framer-motion`)**: 画面遷移やカード表示の滑らかなエフェクト。

### 3. 作り直す部分
* **入力フォーム構造**: 単一の縦長フォームから、スマホでストレスなく進められる「3ステップ・ウィザード形式」へ刷新。
* **計算エンジン (`simulator.ts`)**: 単なる複利計算から、年齢ごとの収入・生活費・ライフイベント支出・投資収益を網羅する「年次キャッシュフローモデル」へ全面拡張。
* **結果画面のストーリーテリング**: 数字の羅列ではなく、「いつ資産がピークを迎えるか」「どのライフイベントで支出が増えるか」「資産枯渇リスクはあるか」を視覚化するダッシュボードへ再構築。

---

## 第2部：データ構造とライフイベント設計

### 1. 新しいデータ構造（TypeScript Interfaces）

```typescript
export type LifeEventType = 'marriage' | 'housing' | 'child' | 'education' | 'car' | 'other';

export interface LifeEvent {
  id: string;
  type: LifeEventType;
  title: string;
  age: number; // 発生年齢
  cost: number; // 総費用（円）
  isLoan?: boolean; // 住宅ローンなどの分割有無
  loanAmount?: number;
}

export interface PlannerInput {
  currentAge: number;        // 現在の年齢 (18〜80)
  targetAge: number;         // 計画終了年齢 (〜100)
  currentAssets: number;     // 現在の金融資産 (円)
  monthlyIncome: number;     // 手取り月収 (円)
  monthlyExpenses: number;   // 毎月の生活費 (円)
  monthlyInvestment: number; // 毎月の積立投資額 (円)
  annualReturnRate: number;  // 想定運用利回り (%)
  lifeEvents: LifeEvent[];   // ライフイベントのリスト
}

export interface YearlyCashFlow {
  age: number;
  year: number;
  income: number;            // 年間手取り収入
  expenses: number;          // 年間生活費
  eventCost: number;         // その年のライフイベント支出
  netSavings: number;        // 差引き貯蓄/取り崩し額 (income - expenses - eventCost)
  investmentGain: number;    // その年の運用益
  totalAssets: number;       // 年末の総資産残高
  principal: number;         // 累計投資元本
  activeEvents: string[];    // 発生しているイベント名
}

export interface PlannerResult {
  yearlyRecords: YearlyCashFlow[];
  finalAssets: number;       // 計画終了時点の資産額
  peakAssets: number;        // 資産の最大値
  peakAge: number;           // 資産ピーク時の年齢
  isDepleted: boolean;       // 資産枯渇（マイナス）の有無
  depletedAge: number | null; // 資産が枯渇する最初の年齢
}
```

### 2. ライフイベントの初期プリセットデータ
ユーザーが手動で入力する手間を軽減するため、一般的なライフイベントのデフォルト値を用意します。
* **結婚**: 28歳 / 300万円
* **第1子誕生**: 30歳 / 50万円
* **住宅購入**: 35歳 / 4,000万円（頭金500万円＋ローン）
* **子ども大学進学**: 48歳 / 500万円（4年間で分割も可）

---

## 第3部：人生全体のキャッシュフロー計算ロジック

### 1. 年次シミュレーションのアルゴリズム
現在の年齢から目標年齢（例：100歳）まで、1歳刻みで以下のループを実行します。

1. **年間収入の計算**:
   $$\text{AnnualIncome} = \text{MonthlyIncome} \times 12$$
2. **年間生活費の計算**:
   $$\text{AnnualExpenses} = \text{MonthlyExpenses} \times 12$$
3. **ライフイベント費用の集計**:
   その年齢に該当する `lifeEvents` の費用合計を算出。
4. **投資元本と運用益の更新（月次複利の年次集約）**:
   前年末の資産残高に対し、1年間毎月の積立額と月次複利を適用し、年末時点の総資産および運用益を算出。
5. **キャッシュフローの差引き**:
   $$\text{YearEndAssets} = \text{StartAssets} + \text{AnnualIncome} - \text{AnnualExpenses} - \text{EventCost} + \text{InvestmentGain}$$
6. **資産枯渇判定**:
   `YearEndAssets < 0` となった場合、その年齢を `depletedAge` として記録し、フラグを立てる。

---

## 第4部：UI/UXと画面構成設計

### 1. 3ステップ入力画面（スマホ最適化）
* **ステップ1：基本プロフィール**（現在の年齢、計画終了年齢、現在の資産）
* **ステップ2：毎月の家計設定**（手取り月収、生活費、毎月の積立額、想定利回り）
* **ステップ3：ライフイベント選定**（結婚・住宅・子どもの予定をカード選択式で簡単に追加・編集）

### 2. 結果画面・資産推移グラフの構成
* **サマリーカード**: 「〇〇歳時点の予想資産」「資産のピーク」「資産枯渇リスクの有無」を大きく表示。
* **ライフイベント連動エリアチャート (Recharts)**: グラフ軸上に結婚💍や住宅🏠のアイコンを配置し、「どのイベントで資産がどう動くか」を視覚化。
* **「もしも」シミュレーションパネル**: 「毎月の積立を+2万円にしたら？」「住宅購入を3年遅らせたら？」のボタンをタップするだけで、即座に結果が再計算されるインタラクティブUI。

---

## 第5部：MVP範囲・実装手順・テスト計画

### 1. MVP（最初に実装する範囲）
* 3ステップの入力ウィザードフォーム
* ライフイベント（結婚・住宅・子ども・老後）を反映した年次キャッシュフロー計算エンジン
* 資産推移のエリアチャートと年齢別詳細テーブル
* 積立額変更・開始年齢変更の「もしも」比較機能

### 2. 将来追加する機能（Ver.2以降）
* インフレ率（物価上昇）の考慮トグル
* パートナーとの合算プラン共有機能
* 詳細なPDFレポート出力

### 3. 実装手順と優先順位
1. **フェーズ1（計算エンジン刷新）**: `simulator.ts` をライフイベント対応のキャッシュフローモデルに拡張し、単体テストを実施。
2. **フェーズ2（入力ウィザード作成）**: スマホで快適に操作できる3ステップフォームを実装。
3. **フェーズ3（結果ダッシュボード構築）**: グラフ描画と「もしも」比較機能を結合。
4. **フェーズ4（総合動作確認・スマホ検証）**: 実機・プレビューでのレスポンシブ検証とエラーハンドリング強化。

### 4. 計算ロジックのテスト項目
* **基本複利テスト**: 積立額0円・利回り0%の場合、単純に資産＋（収入-支出）×年数になること。
* **ライフイベント控除テスト**: 特定の年齢で高額なイベント費が発生した際、資産残高が正確に減少し、必要に応じて枯渇判定が働くこと。
* **エッジケーステスト**: 極端な高齢入力、マイナス値入力、期間が短すぎる場合のバリデーションエラーが適切にハンドリングされること。

---
以上が、人生全体マネープランシミュレーターの正式な実装計画・データ構造・計算仕様書です。
